{% extends 'base.html.twig' %}

{% block body %}
    <h2>Edit Comment</h2>
    {{ form_start(form) }}
        {{ form_row(form.comment_text) }}
        <button type="submit" class="btn btn-primary">Save</button>
    {{ form_end(form) }}
{% endblock %}
