<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Login</title>

    <meta name="viewport" content="width=device-width, initial-scale=1">
    
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/styleHome.css">
    <link rel="stylesheet" href="/css/login.css">

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    
</head>
<body>

{% include '/nav.html.twig' %}

<div class="container d-flex justify-content-center align-items-center" style="margin-top : 130px;">
    <div class="row border rounded-5 p-3 bg-white shadow box-area">
        <div class="col-md-6 rounded-4 d-flex justify-content-center align-items-center flex-column left-box" style="background: #fff;">
            <div class="featured-image mb-3">
                <img src="/images/LoginRed.png" class="img-fluid" style="width: 600px;">
            </div>
        </div>
        <div class="col-md-6 right-box">
            <div class="row align-items-center">
                <div class="header-text mb-3">
                    <h2 class="text-danger">Connexion</h2>
                </div>
                {% if error %}
                    <div class="alert alert-danger p-1">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                {% endif %}
                <form  action="{{ path('login') }}" method="post">
                    <div class="form-group mb-3">
                        <label for="inputEmail" class="form-label">Email :</label>
                         <input class="form-control" id="inputEmail" name="_username" value="{{ last_username }}" required autofocus>
                    </div>
                    <div class="form-group mb-3">
                        <label for="inputPassword" class="form-label">Mot de passe :</label>
                        <input type="password" class="form-control" id="inputPassword" name="_password" required>
                    </div>
                    <div class="mb-3 d-flex justify-content-between align-items-center">
                        <div class="checkbox" id="rememberMeContainer">
                            <label>
                                <input type="checkbox" name="_remember_me" id="_remember_me" style="accent-color: red;">
                                Se souvenir de moi
                            </label>
                        </div>
                        <div class="forgot">
                            <a class="text-danger" href="#">Mot de passe oublié?</a>
                        </div>
                    </div>
                <div class="input-group mb-3">
                    <button type="submit" class="btn btn-lg btn-danger w-100 fs-6">Connexion</button>
                </div>
                </form>
                <div class="row">
                    <small>Vous n'avez pas encore du compte ? <a class="btn btn-outline-danger p-1" href="{{ path('signUp') }}">Créez-en un</a></small>
                </div>
            </div>
        </div>
    </div>
</div>


<script>
document.addEventListener("DOMContentLoaded", function() {

  let input = document.cookie.split("=");
  // Check if the 'cookieConsent' cookie is set to 'ACCEPTED'
  if (input[0] == "ACCEPTED") {
    // Show the "Remember me" checkbox
    document.getElementById('rememberMeContainer').style.display = 'flex';
  } else {
    // Hide the "Remember me" checkbox
    document.getElementById('rememberMeContainer').style.display = 'none';
  }
});
</script>

</body>
</html>
