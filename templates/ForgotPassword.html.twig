<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Mot de passe oublié</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        .hm:focus {
            outline: none;
            border-color: rgb(251, 251, 251);
            box-shadow: 0 0 0 0.15rem rgba(167, 1, 1, 0.5);
            }
    </style>
</head>
<body>

{% include '/nav.html.twig' %}


<div class="container d-flex justify-content-center align-items-center" style="margin-top : 100px; max-width: 800px;">
    <div class="row border rounded-5 p-2 bg-white shadow box-area">
        <div class="col-md-6 rounded-4 d-flex justify-content-center align-items-center flex-column left-box" style="background: #fff;">
            <div class="featured-image mb-3">
                <img src="/images/emailVerification.png" class="img-fluid" style="width: 600px;">
            </div>
        </div>
        <div class="col-md-5 right-box">
            <div class="row align-items-center" style="margin-top : 35px;">
                <div class="header-text mb-4 p-0">
                    <h2 class="text-danger">Mot de passe oublié</h2>
                </div>
                <div class="card-body">
                    {% for message in app.flashes('success') %}
                        <div class="text-success m-1">
                            {{ message }}
                        </div>
                    {% endfor %}
                    {% for message in app.flashes('msg') %}
                        <div class="text-danger m-1">
                            {{ message }}
                        </div>
                    {% endfor %}

                    {% for message in app.flashes('error') %}
                        <div class="text-danger m-1">
                            {{ message }}
                        </div>
                    {% endfor %}
                    <form action="{{ path('sendResetMail') }}" method="post" id="resetPasswordForm" class="mt-2">
                        <div class="form-group">
                            <input type="email" name="email" class="form-control hm" placeholder="entrez votre email ici" required>
                        </div>
                        <div class="text-center mt-1">
                        <button type="submit" class="btn btn-danger mt-3 mb-4" id="submit">Envoyer</button>
                        </div>
                    </form>
            </div>
        </div>
    </div>
</div>

<script src="/js/forgotPassword.js"></script>

</body>
</html>